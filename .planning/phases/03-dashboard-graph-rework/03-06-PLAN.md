---
wave: 3
depends_on:
  - 03-01-PLAN.md
files_modified:
  - 02-worktrees/webapp-ui/frontend/src/lib/components/DashboardGraph.svelte
  - 02-worktrees/webapp-ui/frontend/src/lib/components/Dashboard.svelte
  - 02-worktrees/webapp-ui/frontend/package.json
autonomous: true
---

# Plan 06: Dashboard Story-Level Graph

## Objective

Create a story-level graph visualization for the dashboard that shows stories as nodes and characters as nodes connected to the stories they appear in. This is the "zoomed out" view described in D-10. Uses a simple manual layout (no d3-force dependency) positioned at the bottom of the dashboard.

## Tasks

### Task 1: Install d3-force dependency

<read_first>
- 02-worktrees/webapp-ui/frontend/package.json
</read_first>

<action>
Add `d3-force` and its types for force-directed graph layout:

```bash
cd 02-worktrees/webapp-ui/frontend && npm install d3-force @types/d3-force
```

This provides `d3.forceSimulation`, `d3.forceLink`, `d3.forceManyBody`, `d3.forceCenter` for automatic node positioning. The force-directed layout handles varying numbers of stories and characters gracefully.
</action>

<acceptance_criteria>
- `package.json` contains `"d3-force"` in dependencies
- `package.json` contains `"@types/d3-force"` in devDependencies
- `npm ls d3-force` exits 0
- TypeScript can resolve `import { forceSimulation, forceLink, forceManyBody, forceCenter } from 'd3-force'`
</acceptance_criteria>

---

### Task 2: Create DashboardGraph component

<read_first>
- 02-worktrees/webapp-ui/frontend/src/lib/components/NodeGraph.svelte (reference for SVG patterns)
- 02-worktrees/webapp-ui/frontend/src/lib/types/index.ts
- 02-worktrees/webapp-ui/frontend/src/lib/api/rest.ts
</read_first>

<action>
Create new file `02-worktrees/webapp-ui/frontend/src/lib/components/DashboardGraph.svelte`.

This component renders a force-directed graph showing stories and characters as nodes, with edges connecting characters to the stories they appear in. Uses d3-force for layout.

```svelte
<script lang="ts">
	import { forceSimulation, forceLink, forceManyBody, forceCenter, forceCollide } from 'd3-force';
	import type { StoryOverviewResponse, StoryOverviewStory, StoryOverviewCharacter } from '$lib/types';
	import { api } from '$lib/api/rest';
	import { storyState } from '$lib/stores/story.svelte';

	// ---- Types ----
	interface GraphNode {
		id: string;
		label: string;
		type: 'story' | 'character';
		x: number;
		y: number;
		fx?: number | null;
		fy?: number | null;
	}

	interface GraphLink {
		source: string | GraphNode;
		target: string | GraphNode;
	}

	interface SimNode extends d3.SimulationNodeDatum {
		id: string;
		label: string;
		type: 'story' | 'character';
	}

	interface SimLink extends d3.SimulationLinkDatum<SimNode> {
		source: SimNode | string;
		target: SimNode | string;
	}

	let nodes = $state<GraphNode[]>([]);
	let links = $state<GraphLink[]>([]);
	let isLoading = $state(true);
	let hoveredNode = $state<string | null>(null);

	// ---- Color palette for character nodes ----
	const PALETTE = [
		'#f87171', '#fb923c', '#fbbf24', '#34d399', '#22d3ee',
		'#60a5fa', '#a78bfa', '#f472b6', '#e879f9', '#94a3b8',
	];

	function getCharColor(index: number): string {
		return PALETTE[index % PALETTE.length];
	}

	async function loadGraph(): Promise<void> {
		isLoading = true;
		try {
			const response = await api.getStoriesOverview();
			buildGraph(response);
		} catch {
			// Graceful failure
			nodes = [];
			links = [];
		} finally {
			isLoading = false;
		}
	}

	function buildGraph(data: StoryOverviewResponse): void {
		if (data.stories.length === 0 && data.characters.length === 0) {
			nodes = [];
			links = [];
			return;
		}

		// Create nodes
		const graphNodes: SimNode[] = [
			...data.stories.map((s) => ({
				id: s.id,
				label: s.title,
				type: 'story' as const,
			})),
			...data.characters.map((c, i) => ({
				id: `char:${c.name}`,
				label: c.name,
				type: 'character' as const,
			})),
		];

		// Create links from character→story
		const graphLinks: SimLink[] = [];
		for (const c of data.characters) {
			for (const sid of c.story_ids) {
				graphLinks.push({
					source: `char:${c.name}`,
					target: sid,
				});
			}
		}

		// Run force simulation
		const simulation = forceSimulation<SimNode>(graphNodes)
			.force('link', forceLink<SimNode, SimLink>(graphLinks).id((d) => d.id).distance(80))
			.force('charge', forceManyBody().strength(-200))
			.force('center', forceCenter(300, 150))
			.force('collide', forceCollide<SimNode>().radius(40))
			.stop();

		// Run simulation synchronously for 120 ticks (no animation needed)
		for (let i = 0; i < 120; i++) {
			simulation.tick();
		}

		// Extract positions
		const positioned: GraphNode[] = graphNodes.map((n) => ({
			id: n.id!,
			label: n.label,
			type: n.type,
			x: n.x ?? 0,
			y: n.y ?? 0,
		}));

		// Normalize positions to start from padding
		const PAD = 40;
		const minX = Math.min(...positioned.map((n) => n.x));
		const minY = Math.min(...positioned.map((n) => n.y));
		for (const n of positioned) {
			n.x = n.x - minX + PAD + 30;
			n.y = n.y - minY + PAD + 30;
		}

		nodes = positioned;
		links = graphLinks.map((l) => ({
			source: typeof l.source === 'object' ? (l.source as SimNode).id! : (l.source as string),
			target: typeof l.target === 'object' ? (l.target as SimNode).id! : (l.target as string),
		}));
	}

	function getNodePos(id: string): { x: number; y: number } {
		const n = nodes.find((n) => n.id === id);
		return n ? { x: n.x, y: n.y } : { x: 0, y: 0 };
	}

	function getLinkedNodeIds(nodeId: string): Set<string> {
		const ids = new Set<string>();
		for (const l of links) {
			const s = typeof l.source === 'string' ? l.source : (l.source as GraphNode).id;
			const t = typeof l.target === 'string' ? l.target : (l.target as GraphNode).id;
			if (s === nodeId) ids.add(t);
			if (t === nodeId) ids.add(s);
		}
		return ids;
	}

	function handleNodeClick(node: GraphNode): void {
		if (node.type === 'story') {
			storyState.setActiveStory(node.id);
		}
		// Character nodes don't navigate anywhere
	}

	// Load on mount
	import { onMount } from 'svelte';
	onMount(() => {
		loadGraph();
	});
</script>

<div class="dashboard-graph">
	{#if isLoading}
		<div class="graph-loading">
			<div class="spinner"></div>
			<p>Loading graph...</p>
		</div>
	{:else if nodes.length === 0}
		<!-- No graph when no stories exist (D-13) -->
	{:else}
		<h4 class="graph-title">Story Universe</h4>
		<svg class="graph-svg" viewBox="0 0 600 300" preserveAspectRatio="xMidYMid meet">
			<!-- Links -->
			{#each links as l, i}
				{@const sourcePos = getNodePos(typeof l.source === 'string' ? l.source : (l.source as GraphNode).id)}
				{@const targetPos = getNodePos(typeof l.target === 'string' ? l.target : (l.target as GraphNode).id)}
				<line
					x1={sourcePos.x} y1={sourcePos.y}
					x2={targetPos.x} y2={targetPos.y}
					class="dash-link"
				/>
			{/each}

			<!-- Nodes -->
			{#each nodes as n (n.id)}
				<!-- svelte-ignore a11y_click_events_have_key_events -->
				<!-- svelte-ignore a11y_no_static_element_interactions -->
				<g
					class="graph-node"
					class:graph-node-hovered={hoveredNode === n.id}
					onclick={() => handleNodeClick(n)}
					onmouseenter={() => (hoveredNode = n.id)}
					onmouseleave={() => (hoveredNode = null)}
				>
					{#if n.type === 'story'}
						<rect
							x={n.x - 40} y={n.y - 16}
							width="80" height="32"
							rx="6"
							class="story-rect"
						/>
						<text
							x={n.x} y={n.y + 1}
							class="node-label story-label"
							text-anchor="middle"
							dominant-baseline="middle"
						>
							{n.label.length > 12 ? n.label.slice(0, 11) + '…' : n.label}
						</text>
					{:else}
						{@const charIdx = nodes.filter((nn) => nn.type === 'character').indexOf(n)}
						<circle
							cx={n.x} cy={n.y} r="20"
							fill={getCharColor(charIdx)}
							class="char-rect"
						/>
						<text
							x={n.x} y={n.y + 1}
							class="node-label char-graph-label"
							text-anchor="middle"
							dominant-baseline="middle"
						>
							{n.label.charAt(0).toUpperCase()}
						</text>
					{/if}
				</g>
			{/each}
		</svg>
	{/if}
</div>

<style>
	.dashboard-graph {
		margin-top: 32px;
		max-width: 1200px;
		margin-left: auto;
		margin-right: auto;
	}

	.graph-title {
		font-size: 12px;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: var(--text-muted, #9ca3af);
		margin: 0 0 12px;
	}

	.graph-svg {
		width: 100%;
		height: 300px;
		background-color: var(--sidebar-bg, #111827);
		border: 1px solid var(--border-color, #374151);
		border-radius: 8px;
	}

	.graph-loading {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		height: 200px;
		gap: 8px;
		color: var(--text-muted, #9ca3af);
	}

	.spinner {
		width: 24px;
		height: 24px;
		border: 2px solid var(--text-faint, #6b7280);
		border-top-color: var(--text-primary, #e5e7eb);
		border-radius: 50%;
		animation: spin 0.8s linear infinite;
	}

	@keyframes spin {
		to { transform: rotate(360deg); }
	}

	/* ---- Links ---- */
	.dash-link {
		stroke: var(--text-faint, #6b7280);
		stroke-width: 1;
		opacity: 0.3;
	}

	/* ---- Nodes ---- */
	.graph-node {
		cursor: pointer;
		transition: opacity 150ms ease;
	}

	.story-rect {
		fill: var(--hover-bg, #1f2937);
		stroke: var(--border-color, #374151);
		stroke-width: 1.5;
		transition: fill 150ms ease, stroke 150ms ease;
	}

	.graph-node:hover .story-rect {
		fill: #374151;
		stroke: #6366f1;
	}

	.char-rect {
		opacity: 0.85;
		stroke: var(--sidebar-bg, #111827);
		stroke-width: 2;
	}

	.graph-node:hover .char-rect {
		opacity: 1;
		stroke-width: 3;
	}

	.node-label {
		font-size: 11px;
		font-weight: 600;
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
		pointer-events: none;
	}

	.story-label {
		fill: var(--text-primary, #e5e7eb);
	}

	.char-graph-label {
		fill: #ffffff;
		font-size: 12px;
		font-weight: 700;
	}
</style>
```
</action>

<acceptance_criteria>
- File `02-worktrees/webapp-ui/frontend/src/lib/components/DashboardGraph.svelte` exists
- Component uses `d3-force` (`forceSimulation`, `forceLink`, `forceManyBody`, `forceCenter`, `forceCollide`)
- Stories rendered as rounded rectangles, characters as colored circles
- Edges connect characters to the stories they appear in
- Story nodes are clickable and call `storyState.setActiveStory(node.id)`
- Character nodes show initial letter (first character of name, uppercased)
- Story node labels truncated to 12 chars with ellipsis
- Force simulation runs 120 ticks synchronously (no animation)
- Positions are normalized to start from padding offset
- Loading state shows spinner
- Empty state (no stories) renders nothing (D-13)
- Component loads overview data via `api.getStoriesOverview()` on mount
- SVG uses `viewBox="0 0 600 300"` with `preserveAspectRatio="xMidYMid meet"` for responsive sizing
- Uses CSS custom properties for dark/light theme support
- Hover highlights nodes (story rect border changes to indigo, character circle becomes brighter)
</acceptance_criteria>

---

### Task 3: Wire DashboardGraph into Dashboard

<read_first>
- 02-worktrees/webapp-ui/frontend/src/lib/components/Dashboard.svelte
- 02-worktrees/webapp-ui/frontend/src/lib/components/DashboardGraph.svelte
</read_first>

<action>
Add the `DashboardGraph` component to the `Dashboard.svelte` component. It should appear BELOW the story grid when stories exist.

1. Add import at the top of the `<script>` block:
```typescript
import DashboardGraph from './DashboardGraph.svelte';
```

2. In the template, after the `{#if overviewStories.length > 0}` block that renders the story grid (after its closing `{/if}`), add:
```svelte
{#if overviewStories.length > 0}
    <DashboardGraph />
{/if}
```

This places the graph below the story grid, only when there are stories (D-13: no graph in empty state).

The `<DashboardGraph />` should be inside the `.dashboard-content` div, after the `.story-grid` div.
</action>

<acceptance_criteria>
- `Dashboard.svelte` imports `DashboardGraph` from `./DashboardGraph.svelte`
- `<DashboardGraph />` is rendered inside `.dashboard-content`, after the story grid
- DashboardGraph only appears when `overviewStories.length > 0`
- DashboardGraph does NOT appear in empty state (no stories)
</acceptance_criteria>

## Verification

1. Start both frontend and backend
2. With NO stories → dashboard shows empty state, no graph visible
3. Create 2-3 stories with different characters (via dashboard or API)
4. Return to dashboard → story grid shows cards, AND below it the "Story Universe" graph appears
5. Graph should show:
   - Story nodes as rounded rectangles with titles
   - Character nodes as colored circles with initials
   - Lines connecting characters to their stories
6. Click a story node in the graph → should switch to editor with that story active
7. Return to dashboard → graph should still be there
8. Resize the window → graph should scale responsively (SVG viewBox)
9. Test with dark and light themes

## must_haves

- Force-directed graph shows stories and characters (D-10)
- Stories as larger nodes, characters as smaller colored nodes
- Edges connect characters to the stories they appear in
- Story nodes are clickable → switch to editor (D-08)
- No graph shown in empty state (D-13)
- Graph uses d3-force for layout (installed as dependency)
- Responsive SVG that scales with container width
