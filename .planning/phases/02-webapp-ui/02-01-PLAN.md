---
phase: 02-webapp-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 02-worktrees/webapp-ui/backend/pyproject.toml
  - 02-worktrees/webapp-ui/backend/app/__init__.py
  - 02-worktrees/webapp-ui/backend/app/main.py
  - 02-worktrees/webapp-ui/backend/app/config.py
  - 02-worktrees/webapp-ui/backend/app/models/__init__.py
  - 02-worktrees/webapp-ui/backend/app/models/schemas.py
  - 02-worktrees/webapp-ui/backend/app/models/database.py
  - 02-worktrees/webapp-ui/backend/app/services/__init__.py
  - 02-worktrees/webapp-ui/backend/app/services/llm.py
  - 02-worktrees/webapp-ui/backend/app/services/story.py
  - 02-worktrees/webapp-ui/backend/app/db/migrations/001_initial.sql
autonomous: true

must_haves:
  truths:
    - "FastAPI server starts on port 8000 with uvicorn"
    - "SQLite database is created with stories, nodes, and provenance_spans tables"
    - "LLM client can be created for lmstudio, ollama, openai, and llamacpp backends"
    - "parse_structured() works with Pydantic schemas (StoryStart, StoryContinue, StoryAnalysis)"
    - "run_cycle() produces a CycleResult with draft text and analysis"
  artifacts:
    - path: "02-worktrees/webapp-ui/backend/app/main.py"
      provides: "FastAPI application entry point with CORS middleware"
      exports: ["app"]
    - path: "02-worktrees/webapp-ui/backend/app/models/database.py"
      provides: "SQLite schema creation and connection management with aiosqlite"
      contains: "CREATE TABLE stories"
    - path: "02-worktrees/webapp-ui/backend/app/models/schemas.py"
      provides: "Pydantic schemas for StoryStart, StoryContinue, StoryAnalysis, LLM config"
      exports: ["StoryStart", "StoryContinue", "StoryAnalysis"]
    - path: "02-worktrees/webapp-ui/backend/app/services/llm.py"
      provides: "LLM client factory and parse_structured()"
      exports: ["create_llm_client", "parse_structured"]
    - path: "02-worktrees/webapp-ui/backend/app/services/story.py"
      provides: "run_cycle() story orchestration"
      exports: ["run_cycle", "CycleResult"]
  key_links:
    - from: "02-worktrees/webapp-ui/backend/app/services/llm.py"
      to: "02-worktrees/webapp-ui/backend/app/models/schemas.py"
      via: "imports StoryStart, StoryContinue, StoryAnalysis for structured output"
      pattern: "from app.models.schemas import"
    - from: "02-worktrees/webapp-ui/backend/app/services/story.py"
      to: "02-worktrees/webapp-ui/backend/app/services/llm.py"
      via: "calls parse_structured() for analysis and continuation"
      pattern: "from app.services.llm import parse_structured"
    - from: "02-worktrees/webapp-ui/backend/app/main.py"
      to: "02-worktrees/webapp-ui/backend/app/models/database.py"
      via: "initializes database on startup"
      pattern: "init_db"
---

<objective>
Scaffold the FastAPI backend with SQLite database, LLM service layer, and story generation pipeline.

Purpose: Create the entire Python backend that the frontend will communicate with. Ports the existing marimo app's LLM client factory, structured output parsing, Pydantic schemas, and story generation cycle into a clean FastAPI service architecture. Establishes the SQLite database with the tree-based story model that supports branching (adjacency list with provenance spans).

Output: A running FastAPI server at localhost:8000 with database initialization, LLM client support for 4 backends, and the story generation pipeline ready for API endpoints.
</objective>

<worktree>
This plan operates on the `webapp-ui` branch via its git worktree.

- **Worktree path:** `02-worktrees/webapp-ui/`
- **Branch:** `webapp-ui` (forked from `00-experiments`)
- **Working directory for all commands:** `02-worktrees/webapp-ui/`
- **Commits go to:** the `webapp-ui` branch (NOT `development`)

All file paths in tasks are relative to the worktree root (`02-worktrees/webapp-ui/`).
When running shell commands, always `cd` into the worktree first or use absolute paths.
Planning artifacts (SUMMARY.md, STATE.md) stay on the `development` branch in `.planning/`.
</worktree>

<execution_context>
@/Users/progressedd/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/progressedd/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webapp-ui/02-CONTEXT.md
@.planning/phases/02-webapp-ui/02-RESEARCH.md
@02-worktrees/demo-marimo-app/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize backend project and create database schema</name>
  <files>
    02-worktrees/webapp-ui/backend/pyproject.toml
    02-worktrees/webapp-ui/backend/app/__init__.py
    02-worktrees/webapp-ui/backend/app/main.py
    02-worktrees/webapp-ui/backend/app/config.py
    02-worktrees/webapp-ui/backend/app/models/__init__.py
    02-worktrees/webapp-ui/backend/app/models/database.py
    02-worktrees/webapp-ui/backend/app/db/migrations/001_initial.sql
  </files>
  <action>
    Create the `backend/` directory inside the webapp-ui worktree at `02-worktrees/webapp-ui/backend/`.

    1. Initialize with `cd 02-worktrees/webapp-ui && mkdir -p backend && cd backend && uv init`, then `uv add fastapi uvicorn[standard] aiosqlite openai ollama pydantic websockets`.

    2. Create `backend/app/config.py` with a Settings class:
       - `DATABASE_URL: str = "sqlite:///./data/stories.db"` (relative to backend/)
       - `CORS_ORIGINS: list[str] = ["http://localhost:5173"]` (Vite dev server)
       - `DEFAULT_BACKEND: str = "lmstudio"`

    3. Create `backend/app/models/database.py`:
       - Use aiosqlite for async SQLite access
       - `async def init_db()` that creates the database file directory if needed and runs the migration SQL
       - `async def get_db()` context manager that returns an aiosqlite connection with WAL mode enabled (`PRAGMA journal_mode=WAL`)
       - Connection should use `row_factory = aiosqlite.Row` for dict-like access

    4. Create `backend/app/db/migrations/001_initial.sql` with the exact schema from RESEARCH.md:
       - `stories` table: id (TEXT PK), title, premise, created_at, updated_at, active_path (JSON array of node IDs)
       - `nodes` table: id (TEXT PK), story_id (FK), parent_id (FK, nullable for root), position (INT), content (TEXT), node_type (TEXT: 'paragraph'|'block'|'sentence'), source (TEXT: 'ai_generated'|'user_written'|'user_edited'|'initial_prompt'), is_draft (INT), created_at
       - `provenance_spans` table: id (TEXT PK), node_id (FK), start_offset (INT), end_offset (INT), source (TEXT), created_at
       - All indexes from RESEARCH.md (idx_nodes_parent, idx_nodes_story, idx_provenance_node)
       - UNIQUE constraint on (story_id, parent_id, position) for sibling ordering

    5. Create `backend/app/main.py`:
       - FastAPI app with title "AI Invasion"
       - CORS middleware allowing origins from config
       - `@app.on_event("startup")` that calls `init_db()`
       - A health check endpoint `GET /api/health` returning `{"status": "ok"}`
       - Do NOT mount static files yet (that's for production, frontend plan handles dev proxy)

    6. Verify: `cd 02-worktrees/webapp-ui/backend && uv run uvicorn app.main:app --port 8000` starts without errors and `curl http://localhost:8000/api/health` returns `{"status":"ok"}`. Check that `backend/data/stories.db` is created with the correct tables.
  </action>
  <verify>
    cd 02-worktrees/webapp-ui/backend && uv run uvicorn app.main:app --port 8000 &
    sleep 3
    curl -s http://localhost:8000/api/health | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='ok'"
    curl -s http://localhost:8000/docs | head -5  # OpenAPI docs should render
    uv run python -c "import sqlite3; conn=sqlite3.connect('data/stories.db'); tables=[r[0] for r in conn.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()]; assert 'stories' in tables and 'nodes' in tables and 'provenance_spans' in tables; print('Tables OK:', tables)"
    kill %1
  </verify>
  <done>FastAPI server starts on port 8000, health endpoint returns ok, SQLite database created with stories/nodes/provenance_spans tables and all indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Port LLM service and story generation pipeline</name>
  <files>
    02-worktrees/webapp-ui/backend/app/models/schemas.py
    02-worktrees/webapp-ui/backend/app/services/__init__.py
    02-worktrees/webapp-ui/backend/app/services/llm.py
    02-worktrees/webapp-ui/backend/app/services/story.py
  </files>
  <action>
    Port the existing marimo app's Pydantic schemas, LLM client factory, and story orchestration logic into the FastAPI service layer.

    1. Create `02-worktrees/webapp-ui/backend/app/models/schemas.py`:
       - Port `StoryStart`, `StoryContinue`, `StoryAnalysis` Pydantic BaseModel classes from app.py lines 390-463 EXACTLY (preserve docstrings — they are the system prompts for structured output)
       - Add `LLMBackendConfig(BaseModel)` with fields: backend (Literal["lmstudio", "ollama", "openai", "llamacpp"]), base_url (Optional[str]), host (Optional[str]), api_key (Optional[str])
       - Add `StoryCreateRequest(BaseModel)` with fields: title (str), premise (str)
       - Add `NodeResponse(BaseModel)` with fields: id, story_id, parent_id, position, content, node_type, source, is_draft, created_at
       - Add `StoryResponse(BaseModel)` with fields: id, title, premise, created_at, updated_at, active_path, nodes (list[NodeResponse])

    2. Create `02-worktrees/webapp-ui/backend/app/services/llm.py`:
       - Port `_normalize_base_url()` from app.py lines 84-88
       - Port `_extract_ids()` from app.py lines 90-105
       - Port `_try_client_models_list()` from app.py lines 107-115
       - Port `_try_ollama_list_models()` from app.py lines 117-152
       - Create `create_llm_client(config: LLMBackendConfig) -> OpenAI` factory — ports app.py lines 160-206 but uses LLMBackendConfig instead of marimo form values. Support lmstudio, ollama, openai, llamacpp (NOT azure — deferred per user decision).
       - Create `async def list_models(config: LLMBackendConfig) -> tuple[list[str], str | None]` that creates a client and returns available models
       - Port `parse_structured()` from app.py lines 359-381 — make it async using `asyncio.to_thread()` since the OpenAI SDK is synchronous
       - Create `async def warmup_model(config: LLMBackendConfig, model: str) -> tuple[bool, str, float]` that sends a tiny request and returns (success, info_message, elapsed_seconds) — port from app.py lines 299-313

    3. Create `02-worktrees/webapp-ui/backend/app/services/story.py`:
       - Port `CycleResult` dataclass and `run_cycle()` from app.py lines 697-735
       - Make `run_cycle()` async (it calls `parse_structured` which is now async)
       - Signature: `async def run_cycle(client, *, model, premise, story_text, temperature_continue=0.2, temperature_analyze=0.2) -> CycleResult`
       - The function does: analyze story (StoryAnalysis) → generate next paragraph (StoryContinue) → return CycleResult(draft_next, analysis)

    4. Create empty `backend/app/services/__init__.py` and `backend/app/models/__init__.py`.
  </action>
  <verify>
    cd 02-worktrees/webapp-ui/backend && uv run python -c "
from app.models.schemas import StoryStart, StoryContinue, StoryAnalysis, LLMBackendConfig
from app.services.llm import create_llm_client, parse_structured, list_models, warmup_model
from app.services.story import run_cycle, CycleResult
# Verify schemas have the right fields
assert 'premise' in StoryStart.model_fields
assert 'next_paragraph' in StoryContinue.model_fields
assert 'logline' in StoryAnalysis.model_fields
assert 'next_paragraph_seeds' in StoryAnalysis.model_fields
# Verify LLMBackendConfig
cfg = LLMBackendConfig(backend='lmstudio')
print('All imports and schema validations passed')
"
  </verify>
  <done>Pydantic schemas (StoryStart, StoryContinue, StoryAnalysis, LLMBackendConfig) importable with correct fields. LLM service (create_llm_client, parse_structured, list_models, warmup_model) and story service (run_cycle, CycleResult) importable and structurally correct. Docstrings preserved as system prompts in the schema classes.</done>
</task>

</tasks>

<verification>
- FastAPI app starts with `cd 02-worktrees/webapp-ui/backend && uv run uvicorn app.main:app --port 8000`
- Health endpoint returns 200
- SQLite database has correct schema (3 tables, 3 indexes)
- All Pydantic schemas importable with correct fields
- LLM service functions importable (create_llm_client, parse_structured, list_models, warmup_model)
- Story service functions importable (run_cycle, CycleResult)
- No import errors in the service layer
</verification>

<success_criteria>
- Backend directory exists at `02-worktrees/webapp-ui/backend/` with pyproject.toml and all dependencies installed
- `uv run uvicorn app.main:app` starts the server
- Database auto-creates on startup with stories, nodes, provenance_spans tables
- Schema docstrings preserved exactly from marimo app (they are LLM system prompts)
- 4 backends supported: lmstudio, ollama, openai, llamacpp (NOT azure)
</success_criteria>

<output>
After completion, create `.planning/phases/02-webapp-ui/02-01-SUMMARY.md`
</output>
